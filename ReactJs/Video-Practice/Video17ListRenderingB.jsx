import React from 'react'

export default function  Raju ({person}) {
    return (
        <div>
            <h1>person color is {person.color} person type{person.type} person registration{person.registration}persons Capacity{person.capacity}</h1>

        </div>
    )
}
